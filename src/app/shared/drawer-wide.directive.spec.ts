import { DrawerWideDirective } from './drawer-wide.directive';

describe('DrawerWideDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerWideDirective();
    expect(directive).toBeTruthy();
  });
});
